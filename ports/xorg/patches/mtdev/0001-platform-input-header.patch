Include this platform's evdev UAPI header.

Same event structure, same event codes, same ioctl encoding; only the name of
the header differs.

--- a/include/mtdev.h
+++ b/include/mtdev.h
@@ -33,7 +33,13 @@
 extern "C" {
 #endif
 
+/* This platform's evdev UAPI header. Same event structure, event codes and
+ * ioctl encoding; it is only reached under a different name. */
+#ifdef __LIKEOS__
+#include <sys/input.h>
+#else
 #include <linux/input.h>
+#endif
 
 /* includes available in 2.6.30-rc5 */
 #ifndef BTN_TOOL_QUADTAP
